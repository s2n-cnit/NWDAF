In this folder there are all the starting points of services that are composing the NWDAF. 
Each service can be run indipendently and it is possible to run all the services at the same time using the nwdaf/main.go application.

The key difference is that using nwdaf executable, the services are started in a single process and the NF is registered at the NRF.
